import { HorasPipePipe } from './horas-pipe.pipe';

describe('HorasPipePipe', () => {
  it('create an instance', () => {
    const pipe = new HorasPipePipe();
    expect(pipe).toBeTruthy();
  });
});
